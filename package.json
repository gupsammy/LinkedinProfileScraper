{
  "name": "linkedin-profile-scraper",
  "version": "0.2.0",
  "description": "A Chrome extension to scrape LinkedIn people search results.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "enforce:no-legacy-globals": "node scripts/check_no_legacy_globals.js"
  },
  "dependencies": {
    "jsdom": "^26.1.0"
  }
}
